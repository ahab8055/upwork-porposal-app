# App Components

This directory contains private components used specifically within the app directory routes.
These components are not accessible from outside the app directory due to the underscore prefix.

Example: Components that are tightly coupled to specific pages or layouts in the app directory.
