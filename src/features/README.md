# Features

This directory is for organizing domain-specific modules.
Each feature folder can contain its own components, hooks, and services.

Example structure:
```
/features
  /user-management
    /components
    /hooks
    /services
  /payment
    /components
    /hooks
    /services
```
